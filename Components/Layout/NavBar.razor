@using ClaseIdiomas;
@inject LocalizationResourceManager loc;
<nav class="navbar navbar-expand-lg custom-navbar shadow-lg px-4 py-3">
    <div class="container-fluid">
        <a class="navbar-brand text-gradient" href="#">
            <img src="logo.png" alt="Logo" style="width: 150px;" />
        </a>

        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-between" id="navbarSupportedContent">
            <ul class="navbar-nav d-flex align-items-center gap-4">
                <li class="nav-item">
                    <NavLink class="nav-link custom-link" href="" Match="NavLinkMatch.All">
                        <i class="bi bi-house-door-fill me-2"></i>@loc["inicio"]
                    </NavLink>
                </li>
                <li class="nav-item">
                    @if (Metodos.perfil)
                    {
                        <NavLink class="nav-link custom-link" href="barajas" Match="NavLinkMatch.All">
                            <i class="bi bi-collection me-2"></i>@loc["barajas"]
                        </NavLink>
                    }
                    else
                    {
                        <NavLink class="nav-link custom-link" href="" Match="NavLinkMatch.All" @onclick="aviso">
                            <i class="bi bi-collection me-2"></i>@loc["barajas"]
                        </NavLink>
                    }
                </li>
                <li class="nav-item">
                    @if (Metodos.perfil)
                    {
                        <NavLink class="nav-link custom-link" href="favoritos" Match="NavLinkMatch.All">
                            <i class="bi bi-heart-fill me-2"></i>@loc["fav"]
                        </NavLink>
                    }
                    else
                    {
                        <NavLink class="nav-link custom-link" href="" Match="NavLinkMatch.All" @onclick="aviso">
                            <i class="bi bi-heart me-2"></i>@loc["fav"]
                        </NavLink>
                    }
                </li>
            </ul>

            <div class="d-flex">
                @if (Metodos.perfil)
                {
                    <NavLink class="nav-link" href="perfil" Match="NavLinkMatch.All">
                        <button class="btn btn-profile">
                            <i class="bi bi-person-circle fs-4"></i>
                        </button>
                    </NavLink>
                }
                else
                {
                    <NavLink class="nav-link" href="login" Match="NavLinkMatch.All">
                        <button class="btn btn-profile">
                            <i class="bi bi-person-circle fs-4"></i>
                        </button>
                    </NavLink>
                }
            </div>
        </div>
    </div>
</nav>

@code {
    public void aviso()
    {
        App.Current!.MainPage!.DisplayAlert(loc["aviso"], loc["needlogin"], "OK");
    }
}
